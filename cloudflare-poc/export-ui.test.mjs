import assert from 'node:assert/strict';import ExcelJS from 'exceljs';import {exportWorkbook} from './export-ui.mjs';import {fields} from './dashboard.mjs';
let blob;const oldCreate=URL.createObjectURL,oldRevoke=URL.revokeObjectURL;URL.createObjectURL=b=>(blob=b,'blob:test');URL.revokeObjectURL=()=>{};globalThis.document={createElement:()=>({click(){}})};
try{await exportWorkbook([{kind:'expenses',data:{date:'2026-09-08',amount:12,vendor:'=NotAFormula'},updated_by:'Sample'},{kind:'expenses',data:{date:'2026-08-08',amount:50},updated_by:'Sample'}],fields,{period:'monthly',month:'2026-09'});const book=new ExcelJS.Workbook();await book.xlsx.load(await blob.arrayBuffer());assert.equal(book.worksheets.length,5);const sheet=book.getWorksheet('Expenses');assert.equal(sheet.rowCount,2);const names=sheet.getRow(1).values;assert.equal(sheet.getRow(2).getCell(names.indexOf('amount')).value,12);assert.equal(sheet.getRow(2).getCell(names.indexOf('vendor')).value,'=NotAFormula');console.log('PASS: Excel workbook sheets, monthly filtering, typed amounts and text-only formula-like input');}finally{URL.createObjectURL=oldCreate;URL.revokeObjectURL=oldRevoke;delete globalThis.document;}
